import 'package:minimaxis_app/module/chat/infra/email/interfaces/interface_request.dart';
import 'package:minimaxis_app/module/chat/infra/email/models/login_model.dart';

class LoginService implements IRequest<LoginModel>{
  @override
  Future delete({required LoginModel headers}) {
    throw UnimplementedError();
  }

  @override
  Future get({required LoginModel headers}) {
    throw UnimplementedError();
  }

  @override
  Future post({required LoginModel headers}) {
    throw UnimplementedError();
  }

  @override
  Future update({required LoginModel headers}) {
    throw UnimplementedError();
  }

}