import 'package:minimaxis_app/module/chat/infra/email/interfaces/interface_request.dart';
import 'package:minimaxis_app/module/chat/infra/email/models/register_model.dart';

class RegisterService implements IRequest<RegisterModel>{
  @override
  Future delete({required RegisterModel headers}) {
    throw UnimplementedError();
  }

  @override
  Future get({required RegisterModel headers}) {
    throw UnimplementedError();
  }

  @override
  Future post({required RegisterModel headers}) {
    throw UnimplementedError();
  }

  @override
  Future update({required RegisterModel headers}) {
    throw UnimplementedError();
  }

}